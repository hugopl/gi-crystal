namespace: Test
version: "1.0"

ignore_constants:
  - IGNORED_CONSTANT

types:
  Subject:
    methods:
      block_thread:
        blocks: true
  Struct:
    ignore_fields:
    - ignored_field
  IgnoredEnum:
    ignore: true
  IgnoredFlags:
    ignore: true
